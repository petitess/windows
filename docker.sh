sudo docker pull mcr.microsoft.com/azure-cli:2.57.0
sudo docker ps
sudo deocker run
sudo docker image ls

sudo docker run nginx -Pd  

docker run -Pd nginxdemos/hello

sudo docker run --name nginx -d -p 8080:80
